[project]
name = "project"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.112.0",
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
]
